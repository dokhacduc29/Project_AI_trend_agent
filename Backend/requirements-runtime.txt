# =====================================================================
# requirements-runtime.txt — Thư viện CẦN THIẾT để chạy pipeline trong production
# =====================================================================
# Đây là nguồn sự thật duy nhất cho Docker image (xem Dockerfile stage builder).
# requirements.txt = superset dùng cho môi trường dev (thêm pytest, dashboard...).
#
# Quy tắc: mọi package ở đây PHẢI được import bởi code trong
# ai_trend_agent/domain, /application, /infrastructure, /worker

httpx==0.28.1           # HTTP bất đồng bộ — scrapers, discord_agent
python-dotenv==1.2.2    # Nạp .env — main.py
google-genai==2.7.0     # SDK Gemini MỚI, cung cấp `from google import genai`
                        # Pin 2.7.0 = version đã chạy thật ở local. KHÔNG nâng lên 2.11.x
                        # nếu chưa chạy hết một chu kỳ pipeline: code gọi
                        # `client.aio.models.generate_content()` — API surface phải được
                        # kiểm chứng trước, không suy đoán từ việc `import` thành công.
supabase==2.31.0        # Client Supabase PostgreSQL — supabase_storage.
                        # Nâng 2.11.0 -> 2.31.0 (ADR 0012): 2.11.0 kiểm tra key
                        # phải là JWT ở create_client() -> chối key sb_secret_ mới
                        # (ADR 0010) với "Invalid API key", dù server nhận key đó
                        # (REST thô trả 200). 2.31.0 bỏ check đó. Đã verify: create_client
                        # nhận sb_secret_ + upsert(on_conflict,ignore_duplicates) chạy đúng.

# --- Pin sàn CVE cho dep bắc cầu (Trivy chặn HIGH có bản vá) ---
msgpack>=1.2.1          # Dep bắc cầu (qua supabase/realtime). 1.1.2 dính GHSA-6v7p-g79w-8964
                        # (HIGH, out-of-bounds read). Pin sàn 1.2.1 để Trivy khỏi chặn CI.

# --- [v5.0 / B2] Tầng REST API (SRS mục 6) ---
fastapi==0.141.1        # Framework API — Iron Law L05 bắt buộc dùng FastAPI nếu làm API.
                        # Kéo theo pydantic 2.x (đã có sẵn qua supabase) + starlette.
uvicorn[standard]==0.52.4  # ASGI server chạy app. Extra `standard` thêm uvloop/httptools
                        # (nhanh hơn) và websockets. Worker CronJob KHÔNG dùng gói này,
                        # nhưng dùng chung một image nên vẫn cài — xem ADR 0015.
